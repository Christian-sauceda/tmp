import { Paper } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Paper
      sx={{
        boxShadow: "none !important",
        borderStyle: "solid",
        borderColor: "divider",
        textAlign: "center",
      }}
    >
    </Paper>
  );
};

export default Footer;
